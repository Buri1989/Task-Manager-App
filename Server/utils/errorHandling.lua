local errorHandling = {}

-- Middleware to handle errors
function errorHandling.handleError(err, req, res, next)
    -- Handle different types of errors and send appropriate responses
end

return errorHandling
